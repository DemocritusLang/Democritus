function main() int
{
  let webaddr string;
  webaddr = "www.smbc-comics.com/comics/2009100";
  
  let i int;
  for(i = 1; i < 10; ++i)
  {
    let webaddr_old string;
    webaddr_old = webaddr;
    let appended string;
    int_to_string(i, appended);
    append_strings(appended, ".gif");
    append_strings(webaddr_old, appended);  
    init_thread("request_from_server", webaddr_old, 1);
  }
return 0;
}
